<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
      <!--ender not on a string with the name
      of a partial, but rather on a user variable of
      class User;9 in this context, Rails automatically
      looks for a partial called _user.html.erb -->

  <%= render @users %>
</ul>

<%= will_paginate %>